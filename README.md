# NonEuclidean FPV experience handoff packet

This artifact contains ordered markdown files for scaffolding a ground-up implementation of the flattened cube VR/FPS exploration environment.

Start with:

```text
docs/llm-handoff/00_index.md
```

The packet is design scaffolding, not implementation code.
